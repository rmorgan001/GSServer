<Window x:Class="GS.Server.Windows.HardwareLimitsV"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:GS.Server.Controls"
        xmlns:windows="clr-namespace:GS.Server.Windows"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes" mc:Ignorable="d" TextElement.FontSize="14" ResizeMode="NoResize" WindowStyle="None" TextOptions.TextFormattingMode="Display"  
        FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto" 
        Title="HardwareLimitsControl"
        d:DataContext="{d:DesignInstance windows:HardwareLimitsVm, IsDesignTimeCreatable=True}" 
        TextElement.Foreground="{DynamicResource MaterialDesignBody}" SizeToContent="WidthAndHeight"
        WindowState="{Binding WindowStates}" ContentRendered="MainWindow_OnContentRendered" WindowStartupLocation="CenterOwner" AllowsTransparency="True" Margin="0">
    <Grid Background="{DynamicResource MaterialDesignPaper}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <md:DialogHost Grid.Row="1"  HorizontalAlignment="Center" CloseOnClickAway="True" IsOpen="{Binding IsDialogOpen}" DialogContent="{Binding DialogContent}" Background="Transparent" >
            <controls:HardwareLimits  Margin="3" Background="{DynamicResource MaterialDesignPaper}"/>
        </md:DialogHost>
</Grid>
</Window>
