<UserControl x:Class="GS.Server.Controls.Backlash"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:domain="clr-namespace:GS.Shared.Domain;assembly=GS.Shared"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:skyTelescope="clr-namespace:GS.Server.SkyTelescope"
             d:DataContext="{d:DesignInstance skyTelescope:SkyTelescopeVm}"
             mc:Ignorable="d">
    <StackPanel Margin="0,0,0,0" Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <Label Content="{StaticResource bacLashComp}" />
            <Label Content="{StaticResource bacRa}" />
            <ComboBox Width="45"
                      Margin="5,0,0,0"
                      HorizontalContentAlignment="Right"
                      ItemsSource="{Binding RaBacklashList}"
                      ToolTip="{StaticResource bacRaLash}">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel />
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
                <ComboBox.SelectedItem>
                    <Binding Mode="TwoWay"
                             Path="RaBacklash"
                             UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <domain:NotEmptyValidationRule ValidatesOnTargetUpdated="True" />
                        </Binding.ValidationRules>
                    </Binding>
                </ComboBox.SelectedItem>
            </ComboBox>
            <Label Margin="10,0,0,0" Content="{StaticResource bacDec}" />
            <ComboBox Width="45"
                      Margin="5,0,0,0"
                      HorizontalContentAlignment="Right"
                      ItemsSource="{Binding DecBacklashList}"
                      ToolTip="{StaticResource bacDecLash}">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel HorizontalAlignment="Right" />
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
                <ComboBox.SelectedItem>
                    <Binding Mode="TwoWay"
                             Path="DecBacklash"
                             UpdateSourceTrigger="PropertyChanged">
                        <Binding.ValidationRules>
                            <domain:NotEmptyValidationRule ValidatesOnTargetUpdated="True" />
                        </Binding.ValidationRules>
                    </Binding>
                </ComboBox.SelectedItem>
            </ComboBox>
        </StackPanel>
    </StackPanel>
</UserControl>

