<UserControl x:Class="GS.Server.Controls.Dialogs.FlipDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" >
    <GroupBox Header="{StaticResource btnFlip}" Margin="5">
        <StackPanel Orientation="Vertical">
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,0" Text="{StaticResource msgContinueFlip}"/>
        <StackPanel Orientation="Horizontal" Margin="0 10 0 0">
            <Button IsDefault="True" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding AcceptFlipDialogCommand}" Content="{StaticResource btnAccept}"/>
            <Button IsCancel="True" Margin="8 0 0 0" Style="{StaticResource MaterialDesignFlatButton}" Command="{Binding CancelFlipDialogCommand}" Content="{StaticResource btnCancel}"/>
        </StackPanel>
        </StackPanel>
    </GroupBox>
    </UserControl>
