<UserControl x:Class="GS.Server.Controls.Dialogs.ErrorMessageDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GroupBox Header="{StaticResource msgError}" Margin="5">
        <StackPanel Orientation="Vertical">
            <TextBlock x:Name="Message" Margin="0 6 0 0" FontSize="18"/>
            <Button IsDefault="True" Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Center"  Margin="10"
                    Command="{x:Static md:DialogHost.CloseDialogCommand}" Content="{StaticResource btnOk}"/>
        </StackPanel>
    </GroupBox>
</UserControl>
